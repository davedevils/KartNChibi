# 0x0090

| Direction | Name | Client fn | Server symbol | Size | System | Status |
|---|---|---|---|---|---|---|
| S2C | MissionStartAck | sub_47DF30 | MissionPackets::missionStartAck | 8 | mission | PROVEN |
| C2S | StartMission | sub_4835C0 | MissionPackets::parseMissionStart | 4 | mission | PROVEN |

## S2C MissionStartAck

```
+0x00  u32  mission_id
+0x04  u32  gold_after   -> profile blob +0x4AC
size   8
```

Closes the modal FIRST then opens stage 25.
CRASH RISK: `0047DF6A CALL sub_450CA0` then `0047DF79 MOV EDX,[EAX+8]` with no null test.
A mission_id never delivered by a 0x87 dereferences address 0x8.

## C2S StartMission

```
+0x00  u32  mission_id
size   4
```

## Our server

`MissionHandler::handleStartMission`. The lock check runs on the chain list of
[0x0088](0x0088.md) since the second pass of 2026-09-23. Since 2026-09-23 (needs the new image): a locked row is
refused with `MSG_UNKNOWN_ERROR` in place of the `MSG_WAIT` of the confirm, the entry fee of
def 0x18 is charged through `ProgressionPackets::spendGold` while the row is not cleared and
the gold after it rides this ack, a short wallet gets `MSG_NO_MONEY`. No [0x0120](0x0120.md)
and no [0x0122](0x0122.md) follow any more, both belong to the rally world of stage 11 and the
0x0120 path of 22 to 33 COL points overran the 12 slots of the client. Tests `MissionFee`.
