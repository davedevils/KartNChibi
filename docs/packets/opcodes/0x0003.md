# 0x0003

| Direction | Name | Client fn | Server symbol | Size | System | Status |
|---|---|---|---|---|---|---|
| S2C | OpenCharacterCreatePopup | sub_479220 | PacketBuilder::trigger | 0 | charcreate | PROVEN |

## S2C OpenCharacterCreatePopup

```
payload none, any length is accepted and discarded
```

Body is `return sub_473730(byte_11B4528)`. Hard preconditions:
1. the 0xBF driver catalog must be NON EMPTY, `@0047379a` does IDIV by the entry count,
   an empty catalog is a hardware divide by zero crash
2. at least one entry with rec+0x00 and rec+0x04 both non zero
3. `Popup/RegistDriver/Driver_Back.png` and `Buttons/Common_OK_` must load
Calls `sub_4538B0` FIRST so it closes MSG_LOGIN. Nickname maxlen is 11 wchars.
