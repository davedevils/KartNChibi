# 0x0025

| Direction | Name | Client fn | Server symbol | Size | System | Status |
|---|---|---|---|---|---|---|
| S2C | DeadDialog37 | sub_479AB0 | *SocialPackets::deadDialog37 | var | dead UI | PROVEN |
| C2S | NamePopupActionA | sub_4807B0 | NONE | 2*(n+1) | messenger | PROVEN wire, INFERRED meaning |

## C2S NamePopupActionA and 0x0029 C2S NamePopupActionB

```
+0x00  UTF16LE wstr  text   the global edit Buffer at 0x00E1EFF8
size   2*(len+1)
```

Byte identical shapes, two buttons of popup id 7. Both clear dword_B23390, close all
popups, then raise MSG_WAIT type 0, so an answer is mandatory.
There is NO S2C case 41, and echoing 0x25 back makes `sub_479AB0` read past the end.
Semantics INFERRED only, the text is a nickname. Do not guess add friend vs invite.

## S2C DeadDialog37

```
+0x00  UTF16LE wstr  name   dest 28 bytes, 13 wchars max
 var   ASCII cstr    key    dest 256 bytes
 var   u32           value
size   2*(name+1)+strlen(key)+1+4
```

MIXED ENCODING, wide FIRST then ASCII. All three fields are really read so the stream stays
aligned, but the consumer `nullsub_8 @0x00463CD0` is a bare ret. Zero visible effect.
