# 0x0135

| Direction | Name | Client fn | Server symbol | Size | System | Status |
|---|---|---|---|---|---|---|
| S2C | RewardPopup or LevelUpPopup | sub_47EEF0 | ProgressionPackets::rewardPopup | 28 plus tail | progression | PROVEN |

## S2C RewardPopup or LevelUpPopup

```
+0x00  u32  unused_00     read into a stack local the handler never uses, no reader
+0x04  i32  source_code
+0x08  u32  ctx_a         -> 0x11B44E8, written on -900 -800 -700 only, no read xref in this build
+0x0C  i32  ctx_b         -> 0x11B450C, same, no read xref in this build
+0x10  u32  category
+0x14  u32  unused_14     read into a stack local the handler never uses, no reader
+0x18  u32  unused_18     read into a stack local the handler never uses, no reader
+0x1C  category tail
size   28 plus the tail
```

```
0 -> blob[0x2C] character   -> 0x844720, remove then add
1 -> blob[0x38] kart        -> 0x845228
2 -> blob[0x1C] item        -> 0x847C38
3 -> blob[0x1C] part        -> 0x846030
4 -> NOTHING READ
5 -> blob[0x30] roomcraft   -> 0x863D68, stackable merge when rec+0x08 == 3
6 -> blob[0x84] part blob FIRST, THEN u8 has_slot, THEN blob[0x34] preset when has_slot == 1
7 and above -> NOTHING READ
```

CATEGORY 6 TAIL ORDER IS THE OPPOSITE OF 0x00B7 CATEGORY 6.
DOES NOT CHANGE EXP OR LEVEL. source_code -700 shows the level up banner and reads the
level ALREADY in the profile blob, so S2C 0x003C or 0x000A carrying the NEW level must
arrive FIRST. -900 and -800 latch other flags. Anything else falls to the random item popup.
