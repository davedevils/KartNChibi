# 0x0032

| Direction | Name | Client fn | Server symbol | Size | System | Status |
|---|---|---|---|---|---|---|
| S2C | RoomSlotEnable | sub_479C70 | PacketBuilder::roomSlotEnabled | 8 | room 3D | PROVEN |

## S2C RoomSlotEnable

```
+0x00  u32  slot     MUST be 0..29, NO bounds check at all
+0x04  u32  enabled  stored raw, only tested non zero
size   8
```

Handler calls `sub_4538B0` FIRST then reads. `sub_409F90` is five instructions with no
guard: `IMUL EAX,EAX,0xbd0 / MOV [EAX+ECX+0x6a20],EDX`. Slot 30 lands exactly on
dword_BB7A48, the local ready flag. Must precede the 0x0021 for that slot.
All 30 slots start disabled online.
