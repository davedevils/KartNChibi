# gate: state race
# gate: env KNC_ITEM_TEST=7
# gate: env KNC_ITEM_SHOTS=0.6,1.2,2.2,3.6,4.8
# the rabbit grabs the own kart the run nif carries it along the racing line then drops it
# the race clock captures out_item_NNNNN at the listed seconds after the green light
waitfor race 900
wait 900
quit
